# Useful functions
```js
parseInt('123', 10)
/* converts strings to integers
where 10 is the base (which is the most common and required)
does not preserve decimals
*/

parseFloat('12.222')
/* does not require base
preserves decimals */
```
